---
title: Introduction
description: OLake Documentation
sidebar_label: Introduction
slug: /
hide_table_of_contents: true
---



import Head from '@docusaurus/Head'

<Head>
  {[
    {
      '@context': 'https://schema.org',
      '@type': 'WebSite',
      url: 'https://olake.io/',
      name: 'Fastest Open Source Data Replication Tool',
      description:
        'Fastest open-source tool for replicating Databases to Data Lake in Open Table Formats like Apache Iceberg. Efficient, quick and scalable data ingestion for real-time analytics. Supporting Postgres, MongoDB, MySQL, Oracle and Kafka with 5-500x faster than alternatives.',
      publisher: {
        '@type': 'Organization',
        name: 'OLake'
      },
      potentialAction: {
        '@type': 'SearchAction',
        target: 'https://olake.io/search?q={search_term_string}',
        'query-input': 'required name=search_term_string'
      }
    },
    {
      '@context': 'https://schema.org',
      '@type': 'TechArticle',
      mainEntityOfPage: {
        '@type': 'WebPage',
        '@id': 'https://olake.io/iceberg/query-engine/'
      },
      headline: 'Apache Iceberg Query Engine Support Matrix',
      description:
        'Comprehensive comparison of 16 query engines supporting Apache Iceberg table format with detailed feature analysis (catalogs, DML, MoR/CoW, streaming, format v3, time travel and security).',
      url: 'https://olake.io/iceberg/query-engine/',
      author: {
        '@type': 'Organization',
        name: 'OLake'
      },
      publisher: {
        '@type': 'Organization',
        name: 'OLake',
        url: 'https://olake.io/',
        logo: {
          '@type': 'ImageObject',
          url: 'https://olake.io/img/logo/olake-blue.svg',
          width: 32,
          height: 32
        }
      },
      datePublished: '2025-10-17',
      articleSection: [
        'Apache Iceberg',
        'Query Engine',
        'Support Matrix',
        'Spark',
        'Flink',
        'Trino',
        'Athena',
        'BigQuery',
        'Snowflake'
      ],
      keywords: 'Apache Iceberg, query engines, support matrix, Spark, Flink, Trino, Athena, BigQuery, Snowflake, OLake'
    },
    {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        {
          '@type': 'ListItem',
          position: 1,
          name: 'Home',
          item: 'https://olake.io/'
        },
        {
          '@type': 'ListItem',
          position: 2,
          name: 'Iceberg',
          item: 'https://olake.io/iceberg/'
        },
        {
          '@type': 'ListItem',
          position: 3,
          name: 'Query Engine',
          item: 'https://olake.io/iceberg/query-engine/'
        }
      ]
    },
    {
      '@context': 'https://schema.org',
      '@type': 'FAQPage',
      mainEntity: [
        {
          '@type': 'Question',
          name: 'What is an Iceberg query engine and why is it important for a lakehouse architecture?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'An Iceberg query engine is a processing engine such as Trino, Spark, Flink, or StarRocks that can read and write data stored in Apache Iceberg table format. It is important because Iceberg separates storage from compute, allowing multiple engines to operate on the same data without data duplication. This enables true lakehouse architecture, avoids vendor lock-in, improves scalability, and reduces infrastructure costs.'
          }
        },
        {
          '@type': 'Question',
          name: 'Which query engines are best suited for Apache Iceberg analytics workloads?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'For interactive analytics and BI queries, engines like Trino, Presto, StarRocks, and Impala are commonly used with Apache Iceberg due to their fast SQL performance and high concurrency support. Apache Spark is widely used for batch processing and data transformations, while Apache Flink is preferred for real-time streaming and CDC workloads. The right engine depends on whether the primary need is analytics, ETL, or real-time ingestion.'
          }
        },
        {
          '@type': 'Question',
          name: 'Does Apache Iceberg support time travel across different query engines?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Yes, many Iceberg-compatible query engines support time travel, allowing users to query historical snapshots of tables using SQL syntax such as FOR VERSION AS OF or FOR TIMESTAMP AS OF. This helps with auditing, debugging, data recovery, and reproducible reporting without restoring backups or copying data.'
          }
        },
        {
          '@type': 'Question',
          name: 'How should teams choose the right Iceberg query engine for their data platform?',
          acceptedAnswer: {
            '@type': 'Answer',
            text: 'Teams should evaluate workload type, required DML support such as UPDATE, DELETE, and MERGE, catalog compatibility with systems like Hive Metastore or AWS Glue, and performance needs like query latency and concurrency. Many modern data platforms use multiple query engines on the same Iceberg tables, choosing the best engine per workload instead of relying on a single processing system.'
          }
        }
      ]
    }
  ].map((schema, index) => (
    <script key={index} type="application/ld+json">{JSON.stringify(schema)}</script>
  ))}
</Head>

<IcebergQueryEngines/>