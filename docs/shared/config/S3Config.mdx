
```json title="destination.json"
{
  "type": "PARQUET",
  "writer": {
    "s3_bucket": "olake-s3-test",
    "s3_region": "ap-south-1",
    "s3_access_key": "xxxxxxxxxxxxxxxxxxxx",  // HMAC Access Key if GCS
    "s3_secret_key": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",  // HMAC Secret Key if GCS
    "s3_endpoint": "https://storage.googleapis.com",  // optional for AWS, mandatory for GCS
    "s3_path": "/data"
  }
}
```
