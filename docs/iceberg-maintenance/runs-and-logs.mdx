---
title: Runs and Logs
sidebar_label: Logs & Runs
---

# What is a Run?

A **run** is a single execution of an optimization job (Lite, Medium, or Full) on a specific Iceberg table.

Each time you start an optimization for a table, OLake creates a new run entry. This entry tracks:

- The **table** being optimized
- The **optimization type** (Lite, Medium, or Full)
- The **current status** of the job
- The **start time and duration**
- The **logs** for that execution
- The **Metrics** of that execution

Runs help you understand what has been optimized, when it happened, and whether it completed successfully.

## Run Lifecycle

Every run goes through a few simple states from start to finish:

| State     | Description                                                                 |
|----------|-----------------------------------------------------------------------------|
| Running  | The optimization job is currently in progress for the table.                |
| Success  | The optimization job finished successfully without errors.                  |
| Failed   | The optimization job stopped due to an error. Check the logs for details.   |
| Cancelled| The optimization job was manually stopped by the user.                      |

## Viewing Runs

You can see all runs for a table from the **Tables** page:

1. Go to the **Tables** page in the OLake UI.
2. In the row for the table you are interested in, click the **Actions** menu (three dots).
3. Click **View Logs & Runs**.

![View Runs Button](pathname:///img/docs/iceberg-maintenance/runs-and-logs/view-runs-and-logs-button.webp)

This opens the **Runs** view for that table. The Runs view typically includes:

- The **optimization type** (Lite, Medium, or Full)
- The **current status** of each run
- **Start** and **duration**
- A **Logs** column so you can inspect details for each run
- A **Metrics** column so you can view metrics of each run

![Runs Page](pathname:///img/docs/iceberg-maintenance/runs-and-logs/runs-page.webp)

## Viewing Logs

From the **Runs** view, you can open detailed logs for a specific run:

1. Locate the run you want to inspect.
2. In the **Logs** column for that run, click the **View** button.

![View Logs Button](pathname:///img/docs/iceberg-maintenance/runs-and-logs/view-logs-button.webp)

This opens the **Logs** page for that run. The Logs page is split into:

- **Run Logs** – high-level logs for the overall optimization run.
- **Sub Tasks Logs** – detailed logs for each sub-task that the run is broken into.

The **Run Logs** section gives you an overview of how the run progressed from start to finish.

![Logs Page](pathname:///img/docs/iceberg-maintenance/runs-and-logs/logs-page.webp)

The **Sub Tasks Logs** section shows separate logs for each sub-task within the run, so you can pinpoint issues in specific parts of the process.

![Sub tasks logs view](pathname:///img/docs/iceberg-maintenance/runs-and-logs/sub-tasks.webp)

